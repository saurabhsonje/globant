on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: addition of two no
      #env:
          #NUM: 5
      env:
        x1: 30
        y2: 40
      run: |
          echo ::set-env name=sum::$(($x1+$y2))
    - run: echo $sum
